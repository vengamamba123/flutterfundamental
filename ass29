Question 5: How to move a widget from the top left corner to the right bottom corner by clicking on a button?
Hint 1:- Use an image widget or container.
Hint 2:- Use Align widget for moving.
source code

import 'package:flutter/material.dart';

class MoveWidgetExample extends StatefulWidget {
  @override
  _MoveWidgetExampleState createState() => _MoveWidgetExampleState();
}

class _MoveWidgetExampleState extends State<MoveWidgetExample> {
  // Alignment flag
  Alignment _alignment = Alignment.topLeft;

  void _moveWidget() {
    setState(() {
      // Toggle position
      _alignment = _alignment == Alignment.topLeft
          ? Alignment.bottomRight
          : Alignment.topLeft;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Move Widget Example")),
      body: Stack(
        children: [
          // AnimatedAlign smoothly transitions between positions
          AnimatedAlign(
            alignment: _alignment,
            duration: Duration(seconds: 2),
            curve: Curves.easeInOut,
            child: Container(
              width: 100,
              height: 100,
              color: Colors.blue,
              // You can also use: Image.asset('assets/person_running.gif')
            ),
          ),
          Align(
            alignment: Alignment.bottomCenter,
            child: Padding(
              padding: const EdgeInsets.all(16.0),
              child: ElevatedButton(
                onPressed: _moveWidget,
                child: Text("Move Widget"),
              ),
            ),
          ),
        ],
      ),
    );
  }
}
